package main

// go fmt ./... && go vet ./... && go test && go build nnn.go && time ./nnn
// go fmt ./... && go vet ./... && go test && go build nnn.go && ./nnn && echo top | go tool pprof cpu.prof

import (
	// "github.com/erikbryant/util-golang/primes"
	"fmt"
	"os"
	"runtime/pprof"
)

