package main

// go fmt ./... && go vet ./... && go test && go build nnn && time ./nnn
// go fmt ./... && go vet ./... && go test && go build nnn && ./nnn && echo top | go tool pprof cpu.prof

import (
	// "github.com/erikbryant/util-golang/primes"
	"fmt"
	"os"
	"runtime/pprof"
)

func main() {
    fmt.Printf("Welcome to nnn\n\n")

    f, _ := os.Create("cpu.prof")
    pprof.StartCPUProfile(f)
    defer pprof.StopCPUProfile()

}
